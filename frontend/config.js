// Runtime configuration for the static frontend.
//
// Set this to your Railway backend's public URL (no trailing slash), e.g.
//   window.__API_BASE__ = "https://milestone-1-zomato-production.up.railway.app";
//
// Leave it as an empty string ("") when the frontend is served by the backend
// itself (single origin) at /ui. When the file is opened directly from disk,
// app.js falls back to http://localhost:8000.
window.__API_BASE__ = "https://milestone-1-zomato-production.up.railway.app";
